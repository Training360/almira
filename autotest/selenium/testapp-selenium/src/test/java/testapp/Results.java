package testapp;

// Generated by Selenium IDE
import org.junit.Test;
import org.junit.Before;
import org.junit.After;
import static org.junit.Assert.*;
import static org.hamcrest.CoreMatchers.is;
import static org.hamcrest.core.IsNot.not;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.Dimension;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.Alert;
import org.openqa.selenium.Keys;
import java.util.HashMap;
import java.util.List;
import java.util.ArrayList;
import java.util.Map;
public class Results {
  private WebDriver driver;
  private Map<String, Object> vars;
  JavascriptExecutor js;
  @Before
  public void setUp() {
    driver = new FirefoxDriver();
    js = (JavascriptExecutor) driver;
    vars = new HashMap<String, Object>();
  }
  @After
  public void tearDown() {
    driver.quit();
  }
  public void Test() {
    driver.get("http://localhost:8080/");
    driver.manage().window().setSize(new Dimension(761, 805));
    driver.findElement(By.id("nameInput")).click();
    driver.findElement(By.id("nameInput")).sendKeys("John Doe");
    driver.findElement(By.cssSelector(".btn")).click();
    driver.findElement(By.cssSelector(".form-check:nth-child(2) > .form-check-label")).click();
    driver.findElement(By.name("btnSubmit")).click();
    driver.findElement(By.cssSelector(".form-check:nth-child(2) > .form-check-label")).click();
    driver.findElement(By.name("btnSubmit")).click();
    assertThat(driver.findElement(By.cssSelector(".container > div")).getText(), is("Your result: 1/2"));
  }
  @Test
  public void Results() {
    Test();
    driver.get("http://localhost:8080/results");
    vars.put("numberOfResults", driver.findElements(By.xpath("/html/body/div/table/tbody/tr")).size());
    assertEquals(vars.get("numberOfResults").toString(), "2");
  }
}
